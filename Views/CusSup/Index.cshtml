@model IEnumerable<BigMac.Models.CusSup_m_CusSup>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var i = 0;
}

        <div class="row">
				<div class="box col-sm-12">			                    
                    <div >
                        <p >
                        <a href="@Url.Action("CusSupSave", "CusSup", new { id = 0 })" >
	                        <i class="fa fa-plus" ></i> 
                            Add New @Request.QueryString["type"].ToString()
                        </a>
                        </p>
                    </div>
                </div>
            </div>
			<div class="row">		
				<div class="col-lg-12">
					<div class="box">
						<div class="box-header" data-original-title>
							<h2><i class="fa fa-user"></i><span class="break"></span>@Request.QueryString["type"].ToString() List</h2>
							<div class="box-icon">
								<a href="#" class="btn-setting"><i class="fa fa-wrench"></i></a>
								<a href="#" class="btn-minimize"><i class="fa fa-chevron-up"></i></a>
								<a href="#" class="btn-close"><i class="fa fa-times"></i></a>
							</div>
						</div>
						<div class="box-content">
							<table class="table table-striped table-bordered bootstrap-datatable datatable">
							  <thead>
								  <tr>
                                      <th>Sr.</th>
									  <th>InHouse Code</th>
									  <th>Name</th>
									  <th>Type</th>
                                      <th>Tel</th>
                                      <th>Address</th>
                                      <th></th>
								  </tr>
                                </thead>   
                                <tbody>
                                        @foreach (var item in Model)
                                        {
                                            i = i+1;
                                            <tr>
                                                <td>
                                                    @i
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.inhousecode)
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.cussupname)
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.cussuptype)
                                                </td>        
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.Tel)
                                                </td>
                                                <td>
                                                    @Html.DisplayFor(modelItem => item.address)
                                                </td> 
									            <td class="center">
										            <a class="btn btn-info" href="@Url.Action("CusSupSave", "CusSup", new { id = item.id,type = Request.QueryString["type"] })" title ="Edit Customer">
											            <i class="fa fa-edit"></i>                                            
										            </a>
										            @*<a class="btn btn-danger" href="@Url.Action("Delete", new { id=item.id })" title ="Remove Staff">*@
                                                    <a class="btn btn-danger" href="#" title ="Remove Customer" onclick ='RemoveRow(this,@item.id)'>
											            <i class="fa fa-trash-o "></i> 
										            </a>
									            </td>                                                
                                            </tr>                                            
                                        }
                                </tbody>
                            </table>							   
                        </div> @*box-content*@
                    </div> @*box*@
                </div>
            </div> @*row*@

<script type ="text/javascript">

    function RemoveRow(paramtr, cid) {
        var result = confirm("Are you sure you Want to delete as it will collapse all linkings related with this Customer?");
        if (result == true) {
@*            $.ajax({
                url: '@Url.Action("CampaignDelete", "Campaign")',
                data: { id: cid },
                type: 'POST',
                success: function (data) {
                    if (data == "SUCCESS")
                    { $(paramtr).closest('tr').remove(); }
                    //alert(data);
                }
            });*@
        }
    }
</script>